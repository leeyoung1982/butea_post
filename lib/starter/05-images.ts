export const TITLE = "图像 · 拖拽编辑 + AI 生图 + 资产库";

export const MARKDOWN = `# 图像 · 拖拽编辑 + AI 生图 + 资产库

> Butea 把图片当**一等公民**——拖一张进编辑器,你可以就地缩放、裁切、对齐、配图注,所有操作都不动原始文件。

> 📷 *配图建议位:三张并列示意——左边是图片刚拖进来的初始状态,中间是 hover 显示对齐工具栏 + 4 角 4 边手柄,右边是配上图注的最终效果*

---

## 把一张图弄进编辑器的三种方式

### 1. 拖文件

直接把本地文件拖进编辑器,**自动存到浏览器 IndexedDB**。Markdown 里只插入一个短引用:

\`\`\`markdown
![my-photo](butea-media://abc-123-def)
\`\`\`

**不是 base64**,源码不会被一张图撑爆。

### 2. 工具栏 🖼 → 上传本地文件

效果跟拖一样,只是用按钮代替拖拽。

### 3. 工具栏 🖼 → 粘公网 URL

如果图已经在 CDN(七牛 / 阿里 OSS / Cloudflare R2 / imgur),直接粘 URL。markdown 里就是 \`![alt](https://...)\`。

> [!tip]
> **本地存储 vs CDN URL** 各有优劣:本地存储离线可写、隐私好;CDN URL 直接发布,不需要再上传图床。日常写文用本地,准备发布前再批量上传到图床。

---

## hover 一张图,出现什么

把鼠标移到图片上。

> 📷 *配图建议位:hover 状态的完整截图,标注:① 顶部对齐工具栏 ② 4 个角点 ③ 4 个边点 ④ 左下角实时尺寸 badge*

### 顶部对齐工具栏

5 个图标按钮(深色半透明 pill):

| 图标 | 作用 |
|------|------|
| ⫷ | **靠左** —— \`display:block; margin-right:auto\`,图片独占一行,右侧留白 |
| ≡ | **居中** —— \`display:block; margin:auto\`,水平居中 |
| ⫸ | **靠右** —— \`display:block; margin-left:auto\`,独占一行,左侧留白 |
| ⫻ | **全宽** —— \`display:block; width:100%\`,撑满容器(会重置尺寸/裁切) |
| ⟲ | **重置** —— 清除所有尺寸 / 裁切 / 对齐,回到原图 |

激活态是橙色填充。再点一次同样的按钮 = 取消激活(回到无对齐 inline 默认)。

### 4 个角点 = 等比例缩放

拖任意角点(NW / NE / SW / SE),**整张图按一个 scale 因子同步缩放**——长宽比保持。

主导轴跟踪:看 cursor 在 X 还是 Y 移动得更多,沿那个轴决定缩放比例。手感跟 Figma 一致。

### 4 个边点 = 裁切像素

拖任意边点(N / E / S / W),**图片内容尺寸不变**,只是把那一侧的像素遮起来。

- 拖右/下边向内 → 砍掉那一侧的像素
- 拖左/上边向内 → 砍掉那一侧的像素(并移动可见窗口)
- 拖左/上边**向外** → 把之前裁掉的恢复回来(如果之前裁过)
- 拖右/下边**向外** → 不动(裁不出图片本身没有的像素)

### 左下角实时尺寸

显示:

- 没裁切时:\`400 × 300\`
- 裁切时:\`300 × 200 (从 400 × 300 裁切)\`

---

## 操作哲学:不动原始 blob

**所有图片操作都不修改原始文件。**

- 缩放只是改 markdown 里的 \`<img width="X" height="Y">\` 属性
- 裁切只是包一层 \`<span style="overflow:hidden;width:Xpx;height:Ypx;"><img ... /></span>\`
- 对齐只是在最外层加 CSS

**Ctrl+Z** 撤销每一次拖动,任何时候都能回到原图。原始 blob 在 IndexedDB 里**永远完整**。

> [!info]
> **裁切跟缩放分开存**:裁切是包装 \`<span>\`,缩放是 \`<img width>\`,对齐是 CSS。你可以缩、裁、对齐叠在一起,任意顺序撤销都不会乱。

---

## 图注

两种语法,两个都支持:

### 法 1:用 alt 文字当 caption

\`\`\`markdown
![这是图注文字](butea-media://abc)
\`\`\`

预览渲染成:

> 📷 *配图建议位:figure 渲染示例——图片下方居中显示斜体小灰字图注*

\`alt\` 既是无障碍的替代文字,也是图注。一举两得。

### 法 2:下一行写斜体

\`\`\`markdown
![](butea-media://abc)
*这是图注文字*
\`\`\`

如果图片有 alt 你又想另写图注,优先级是:**italic 下一行 > alt 文字**。

> [!note]
> 占位 alt(\`image\` / \`img\` / \`图片\` / 文件名格式如 \`IMG_1234\`)会被自动跳过,不渲染成图注。

---

## AI 生成插图

工具栏 ✨ **AI ▾** → **生成文中插图**

弹窗里输入提示词,AI 生成一张图插入到光标位置。

> 📷 *配图建议位:AI 生图弹窗截图,展示 prompt 输入框 + 生成中的进度 + 完成后的预览*

**支持的图片 provider**(在 设置 → 图片 provider 配):

- **fal.ai** —— 推荐。代理 GPT image-1、FLUX、Stable Diffusion 等模型,API 一致
- **OpenAI** —— DALL-E 3
- **自定义 OpenAI 兼容**(Stability / Replicate 等)

> [!tip]
> **文字和图片可以用不同 provider**——比如文字用 DeepSeek 省钱,图片用 fal.ai 跑 GPT image-1 质量好。两个 provider 完全独立配置。

---

## AI 生成封面

工具栏 ✨ **AI ▾** → **生成封面**

跟生成插图类似,但有几个不同:

| | 生成插图 | 生成封面 |
|---|---------|---------|
| 提示词 | 你手动写 | 自动从 H1 标题派生 |
| 比例 | 1:1 默认 | 16:9 |
| 质量 | 默认 | 高质量(更贵但更精) |
| 位置 | 插到光标位置 | 插到正文最上面(H1 之后) |

适合写完文章一键配封面。

---

## 资产库 🖼

左侧栏第三个图标。**所有你用过的图都在这**——上传的、AI 生成的、拖入的,统一在一个 grid 里。

> 📷 *配图建议位:资产库面板截图,2 列网格,每张图带名称 + 文件大小 + 时间戳*

### 卡片操作

hover 任意卡片右上角:

| 图标 | 作用 |
|------|------|
| ✂ | **编辑 / 裁剪** —— 弹出 canvas 编辑器,可裁切 + 缩放 + 调 JPEG 质量 |
| 📋 | **复制引用** —— 复制 \`butea-media://...\` 字符串到剪贴板 |
| 🗑 | **删除** —— 从 IndexedDB 移除(注意:已引用此图的 markdown 会变成损坏链接) |

### 插入到编辑器

| 操作 | 效果 |
|------|------|
| **点击图片** | 在编辑器当前光标位置插入 \`![alt](butea-media://...)\` |
| **拖到编辑器** | 在松开鼠标的位置插入(MD 模式)或精确位置(Visual 模式) |

### 上传

资产库顶部 **⬆ 上传图片** 按钮,跟编辑器工具栏的上传等价。

### 容量限制

- 浏览器 IndexedDB 默认配额因浏览器不同,通常 几百 MB 到 几 GB
- **LRU 缓存**:同时活跃的 100 张图片走 blob URL 缓存,超过自动回收(图片本身不删,只是再用时重新生成 blob URL)

---

## 资产库里的编辑器

点 ✂ 编辑按钮弹出的对话框是一个**完整的图片编辑器**:

> 📷 *配图建议位:图片编辑器弹窗截图——左边图片预览 + 可拖拽的裁切框,右边参数面板(最长边 / JPEG 质量 / 覆盖原图 toggle)*

### 裁切

图片预览上有一个可拖拽的矩形裁切框。

- **拖矩形内部** = 移动裁切位置
- **拖矩形 4 角** = 改裁切大小

裁切框是相对原图的,你可以裁出任何区域。

### 缩放(最长边)

下方 4 个 chip:

- **800** —— 适合微博 / 朋友圈 / X 配图
- **1200** —— 适合公众号 / 小红书
- **1920** —— 适合博客封面 / X Long-form 头图
- **原始** —— 不缩放(只裁切)

### JPEG 质量

滑块 60-95。默认 85。**95 是无损视觉等价**,**70 通常肉眼看不出差别但体积小很多**。

### 保存方式

| 选项 | 效果 |
|------|------|
| **保存为新资产** | 创建新条目(\`.edited.jpg\` 后缀),原图保留 |
| **覆盖原图** | 替换原图内容(id 不变),所有引用此图的 markdown 文档**自动看到新版本** |

> [!warning]
> **覆盖原图**会替换浏览器里的字节,这是**不可撤销**的(不像编辑器里的拖动操作)。重要图片建议**保存为新资产**。

---

## 媒体存储的设计哲学

为什么图片要存 IndexedDB 而不是直接 base64 inline 到 markdown?

| | base64 inline | Butea 的 \`butea-media://\` |
|---|---------------|---------------------------|
| markdown 文件大小 | **巨大**(一张图变 1MB 文本) | 极小(只是个 36 字符的 id) |
| 编辑器卡顿 | 严重(CodeMirror 渲染 1MB 字符串) | 无 |
| 发布前还需要 | 还要把 base64 转回二进制再传图床 | 直接传二进制 |
| 跨文档复用 | 一张图复用,源码里出现 N 次 base64 | 一个 id,引用 N 次 |
| 浏览器 storage | localStorage 撑不住 | IndexedDB 容量大 |

唯一缺点是**跨浏览器不同步**——但 BYOK 工具不上服务器,这是必然代价。

---

## 发布前:批量上传到图床

写完文章准备发布时,**markdown 里那些 \`butea-media://\` 引用要换成公网 URL**——否则别人的浏览器看不到。

**导出对话框**里的"图片上传"区:

- **Imgur** —— 免费匿名,每天 ~1250 张
- **GitHub** —— Personal Access Token + public repo,无限,但要会用 git

→ 详细配置见 [#8 设置与开源](#8)

按一下"开始上传",Butea 把所有 \`butea-media://\` 替换成上传后的 URL,**复制结果到剪贴板**直接粘到目标平台。

---

## 关于"已经在 CDN 的图"

如果你已经把图放在自己的 CDN(七牛 / 阿里 OSS / Cloudflare R2),**完全可以跳过 IndexedDB**——直接在工具栏 🖼 → **粘公网 URL**,markdown 里就是 \`![alt](https://your-cdn/...)\`。

这样:

- 文章源码极轻
- 发布时无需上传(URL 已经是公网的)
- 可以跨设备一致(任何浏览器打开都看到图)

**适合**:你已经有图床工作流的高阶用户。

---

*下一站 → #6 一稿多形态 · 7 个平台原生化*
`;
