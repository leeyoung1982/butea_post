export const TITLE = "写作 · MD/Visual 双编辑器与文字样式";

export const MARKDOWN = `# 写作 · MD/Visual 双编辑器与文字样式

> Butea 有两种编辑器,**共享同一份草稿**,随时切换。这一篇覆盖编辑器和文字样式的全部能力。

> 📷 *配图建议位:两种 mode 并排截图——左边 MD 源码,右边可视所见即所得,中间一个切换按钮箭头*

---

## 为什么有两种 mode

| | MD 模式 | 可视模式 |
|---|---------|---------|
| 引擎 | CodeMirror 6 | TipTap 3 |
| 显示 | Markdown 源码 + 语法高亮 | 渲染后的文档 |
| 适合 | 熟手、键盘流、写代码块多的技术稿 | 不熟 markdown、图文混排、所见即所得 |
| 速度 | 极快(纯文本) | 略慢(渲染开销) |

**两种 mode 共享底层的同一份 Markdown 草稿**。你在 MD 里写 \`**加粗**\`,切到可视看到加粗效果。你在可视点加粗按钮,切回 MD 看到 \`**...**\` 标记。

切换不掉数据、不丢格式、不会有"复制粘贴丢样式"的问题。

---

## 工具栏

**两侧工具栏完全一致**——同样的按钮、同样的顺序、同样的图标。换个 mode 不需要重新学习。

从左到右的区段:

> 📷 *配图建议位:工具栏完整截图,加注释标签——分组为"文字格式 / 文字样式 / 媒体 / AI / 历史"*

### 文字格式

| 按钮 | 作用 | 快捷键 |
|------|------|--------|
| **B** | 加粗 | ⌘B / Ctrl+B |
| *I* | 斜体 | ⌘I / Ctrl+I |
| ~~S~~ | 删除线 | — |
| **H2** | 二级标题 | — |
| ❝ | 引用 / blockquote | — |
| ≡ | 无序列表 | — |
| ☷ | 有序列表 | — |
| </> | 代码(智能行内 vs 块) | ⌘E / Ctrl+E |

**键盘 shortcut 三连**(⌘B / ⌘I / ⌘E)是写得最快的方式——不会让光标失焦。

### 文字样式 ▾ 下拉

工具栏中间的 \`T 样式 ▾\`——颜色、字号、行高、对齐一站搞定。

| | 选项 |
|---|------|
| **颜色** | 13 个 swatch(默 / 黑 / 灰 / 浅灰 + 橙 / 红 / 黄 / 绿 / 青 / 蓝 / 紫 / 粉) |
| **字号** | 7 档(极小 12 / 小 14 / 默认 / 中 18 / 大 22 / 特大 28 / 标题 36 px) |
| **行高** | 4 档(紧凑 1.3 / 标准 / 舒适 1.8 / 宽松 2.2) |
| **对齐** | 左 / 中 / 右 / 两端对齐(justify) |

> [!info]
> **平台适应性**:这些样式在**长文平台**(公众号 / 博客 / X Long-form)中完整保留;在**短文平台**(小红书 / X Thread / 微博 / 朋友圈)产出的是纯文本输出,样式自动剥除——这是每个平台的原生行为,不是 bug。

### 媒体

| 按钮 | 作用 |
|------|------|
| 🖼 **图片 ▾** | 上传本地文件 / 粘公网 URL |
| 🎬 **视频** | 嵌入 YouTube / Bilibili / Vimeo / 直链 .mp4 |
| 🔗 **链接** | 普通文字链 + 外链卡片(自动抓 og:title + 缩略图) |
| — **分割线** | 插入 \`---\` |

→ 媒体的深度玩法见 [#5 图像](#5)

### AI

✨ **AI ▾** 下拉:文字改写(5 个动作)+ 图片生成(2 个动作)。

→ 详细见 [#3 灵感与起笔](#3)

### 历史

撤销 / 重做。⌘Z / ⌘⇧Z 也行。

---

## Admonition 卡片

在 markdown 源码里这样写:

> [!tip]
> 这是一个 tip 卡片。

预览渲染成带图标和配色的高亮卡片。

**6 种类型,各有用途**:

> [!info]
> 这是 **info** 卡片。用于普通信息提示、背景知识、补充说明。蓝色调。

> [!tip]
> 这是 **tip** 卡片。用于给读者的小建议、节省时间的捷径、隐藏的快捷键。绿色调。

> [!warning]
> 这是 **warning** 卡片。用于"注意,这一步容易出错"、"以下内容不适合所有人"。橙色调。

> [!danger]
> 这是 **danger** 卡片。用于强烈风险提示、违规警告、合规边界。红色调。

> [!note]
> 这是 **note** 卡片。用于"顺带一提"、"小知识"、可选补充。灰色调。

> [!quote]
> 这是 **quote** 卡片。用于经典引用、采访摘录、值得一记的金句。品牌主色。

**语法记忆**:在 blockquote(\`>\`)的第一行开头写 \`[!类型]\`,然后正文。

---

## 代码块

带语言标签的代码块自动出现 **小标签(左上)+ 复制按钮(右上)**。

\`\`\`typescript
// TypeScript 示例
export const adapter: Adapter = {
  id: "my-platform",
  category: "longform",
  async render(draft, theme) {
    return { kind: "html", html: "..." };
  },
};
\`\`\`

\`\`\`bash
# 命令行示例
npm run dev
\`\`\`

\`\`\`python
# Python 也行
def hello():
    print("Hello from Butea")
\`\`\`

任何语言都会被 [highlight.js](https://highlightjs.org/) 识别并语法高亮。

---

## Markdown 行内格式

可以混用:

- **加粗** —— \`**加粗**\`
- *斜体* —— \`*斜体*\` 或 \`_斜体_\`
- ~~删除线~~ —— \`~~删除线~~\`
- \`行内代码\` —— 用反引号包
- [链接文字](https://example.com) —— \`[文字](url)\`

行内组合可以叠:**加粗里有 *斜体*** 都行。

---

## 表格

GFM 风格表格,语法标准:

\`\`\`markdown
| 列 1 | 列 2 | 列 3 |
|------|------|------|
| 数据 | 数据 | 数据 |
| 数据 | 数据 | 数据 |
\`\`\`

渲染后:

| 列 1 | 列 2 | 列 3 |
|------|------|------|
| 数据 | 数据 | 数据 |
| 数据 | 数据 | 数据 |

**对齐**:列分隔行用 \`:---\` 左对齐、\`:---:\` 居中、\`---:\` 右对齐。

---

## 列表

### 无序列表

- 一级项
- 一级项
  - 二级嵌套(前面 2 个空格 + \`-\`)
  - 二级嵌套
    - 三级嵌套
- 一级项

### 有序列表

1. 第一步
2. 第二步
3. 第三步
   1. 子步骤(同样缩进)
   2. 子步骤
4. 第四步

### Task 列表(GFM)

- [x] 已完成
- [ ] 未完成
- [ ] 未完成

---

## 引用 / blockquote

普通引用:

> 这是一段引用。
> 多行连续会合并成一段。
>
> 中间空行会分成新段落。

嵌套:

> 一级引用
> > 二级引用
> > > 三级引用

---

## HTML inline(进阶)

Butea 的 markdown 管线 **允许内联 HTML**——你可以直接写 HTML 标签,渲染会保留。

例如:

\`\`\`html
<span style="color: #EA580C; font-weight: 600;">高亮文字</span>
\`\`\`

或:

\`\`\`html
<details>
  <summary>点击展开</summary>
  隐藏的内容
</details>
\`\`\`

**何时用**:
- 工具栏样式菜单覆盖不了的微调
- 平台特定的 HTML(如公众号的某些样式)
- 你已经知道自己在干什么

工具栏的 **样式 ▾** 内部就是用这种 HTML 实现的——你不需要手写。

---

## 双编辑器之间的 round-trip 边界

大多数 markdown 元素两个 mode 都能完美互转。极少数边界情况:

| 元素 | MD 写法 | 可视显示 | 来回切换 |
|------|---------|---------|---------|
| 加粗 / 斜体 / 删除线 | \`**\` / \`*\` / \`~~\` | ✓ 渲染 | ✓ 无损 |
| 链接 / 图片 | \`[]()\`/ \`![]()\` | ✓ 渲染 | ✓ 无损 |
| 代码块 | \`\`\`lang | ✓ 渲染 | ✓ 无损 |
| 表格 | GFM 表格语法 | ✓ 渲染 | ✓ 无损 |
| Admonition | \`> [!type]\` | ✓ 渲染为卡片 | ⚠ 可视模式仍显示为 blockquote(下一版改善) |
| 复杂 HTML 块 | inline HTML | ✓ 渲染 | ⚠ 可视模式当作 HTML node 整体处理 |
| 自定义 button / dialog | 不建议在草稿里写 | — | — |

**建议**:复杂样式调整放在 MD 模式做(更可控),日常写文用可视模式更顺手。

---

## Frontmatter(可选)

你可以在文档最顶端写 YAML frontmatter:

\`\`\`markdown
---
title: 我的文章
date: 2026-05-15
tags: [独立开发, AI]
---

# 正文从这里开始
\`\`\`

预览时 frontmatter 不渲染(自动剥除),但导出时一些平台(Hugo / Jekyll / Astro)需要它。

---

## 实用快捷键速查

| 操作 | macOS | Windows/Linux |
|------|-------|---------------|
| 加粗 | ⌘ B | Ctrl B |
| 斜体 | ⌘ I | Ctrl I |
| 代码 | ⌘ E | Ctrl E |
| 撤销 | ⌘ Z | Ctrl Z |
| 重做 | ⌘ ⇧ Z | Ctrl Shift Z |
| 搜索 | ⌘ F(在 MD 模式) | Ctrl F |

---

## 一些"细节但重要"

- **autosave** 写一会儿停一下,自动存到 IndexedDB。无需手动保存
- **预览实时刷新** 但有 120ms 防抖,不会跟你打字一样快(避免卡顿)
- **MD 模式下图片** 不显示在源码里——会**显示在 markdown 文本下方**(inline 缩略图),配文字样式工具栏的图片编辑能力(下一篇讲)
- **可视模式下选中文字** 还能用 ⌘B / ⌘I 加粗斜体,跟原生富文本一致

---

*下一站 → #5 图像 · 拖拽编辑 + AI 生图 + 资产库*
`;
