export const TITLE = "文档与资产管理 · 工作的根";

export const MARKDOWN = `# 文档与资产管理 · 工作的根

> 你的草稿、图片、版本,都在浏览器里你自己的 IndexedDB。不上服务器、不收订阅、不会因为我们倒闭你内容就没了。

> 📷 *配图建议位:文档库面板的截图——本地 / 外部 / 回收站三个 tab,本地 tab 下有多个文档卡片,顶部一个"编辑中"绿色徽标*

---

## 本地优先哲学

Butea 是一个**纯前端**应用。没有服务器、没有用户系统、没有云存储。

| | Butea | 典型 SaaS 写作工具 |
|---|-------|------------------|
| 你的内容存在哪 | 你的浏览器 IndexedDB | 我们的服务器 |
| 离线可用 | ✅ 完全可用 | ❌ 大多不行 |
| 跨设备同步 | 手动(或 Obsidian 中转) | 自动 |
| 公司倒了你的内容 | ✅ 还在你浏览器 | ❌ 没了 |
| 隐私 | ✅ 我们看不到 | ⚠ 看协议 |
| 月费 | 永久免费 | $5-30/月 |

**Butea 把"内容在你自己手里"作为不妥协的设计前提。** 跨设备同步交给 Obsidian / Git / 别的工具,我们不抢这个生态位。

---

## 文档库 📁

左侧栏第二个图标。**三个 tab**:

> 📷 *配图建议位:文档库面板特写,展示三个 tab 的图标和高亮状态*

### 本地

存在浏览器 IndexedDB 的所有文档。

**功能**:

- ➕ **新建文档** —— 顶部按钮
- 📥 **导入文件** —— 顶部按钮
- 📄 文档列表 —— 按更新时间倒序
- 🟢 **编辑中徽标** —— 当前正在编辑的那篇固定在顶部
- 🗑 **删除** —— hover 卡片右侧出现垃圾桶图标

### 外部

外部笔记源(逐步接入中):

| 源 | 状态 |
|---|------|
| **Obsidian**(Local REST API) | ✅ v0.4 支持 |
| **Notion** | 🚧 v0.5 计划(OAuth + API) |
| **Google Drive** | 🚧 v0.5 计划 |

→ Obsidian 详细在下面

### 回收站

删除的文档**不立刻消失**,先进回收站,30 天后自动永久删除。

**操作**:

- ↩️ **恢复** —— 把文档拉回本地 tab,重新可编辑
- 🗑 **永久删除** —— 立刻清除(不可逆)

剩余天数显示:"删除于 3 小时前 · 剩 30 天"。

---

## "编辑中" 徽标和智能排序

为什么需要这个?

**问题**:你打字的时候 autosave 每 600ms 更新一次文档的 \`updatedAt\` 字段。如果按"最近更新"排序,**你当前的文档会一直在最上面跳来跳去**——一边打字一边看文档库,会很混乱。

**Butea 的解法**:

- 当前正在编辑的文档**固定钉在顶部**,带绿色 "编辑中" 徽标
- 其他文档按 \`updatedAt\` 倒序
- 切换到别的文档时,旧文档掉回正常列表位置,新文档钉顶

这样**列表稳定 + 当前活跃文档永远显眼**。

---

## 切换文档

点任意文档卡片 → 编辑器加载那篇文档。

**保护机制**:

- 切换之前,当前文档已经 autosave 到 IDB(不会丢未保存改动)
- 切换瞬间 toast 提示:"已切换到《XXX》"
- 平台适配版本也跟着切(每个文档有独立的适配缓存)

---

## 导入

支持四种格式:

| 格式 | 处理 |
|------|------|
| **.md** | 直接读 |
| **.txt** | 当 markdown 处理 |
| **.html** | 用 turndown 转 markdown(保留标题 / 链接 / 列表 / 图片) |
| **.docx**(Word) | 用 mammoth.js 提取文本 + 简单格式,转 markdown |

**怎么用**:文档库本地 tab 顶部 → **从本地文件导入** → 文件选择器。

> [!warning]
> **.docx 转 markdown 是有损的**——复杂格式(表格嵌套 / 字体 / 颜色)可能丢失。导入完检查一遍。

---

## Obsidian 双向同步

如果你重度用 Obsidian,Butea 可以**直接读写你的 Vault**。

### Step 1 · 装 Obsidian 插件

Obsidian → 设置 → 第三方插件 → 关闭安全模式 → 浏览 → 搜 \`Local REST API\` → 安装 + 启用。

### Step 2 · 配置 API key

Obsidian → 设置 → Local REST API:

- 生成 API key(随机长串)
- 端口:默认 27124
- HTTPS:默认开

### Step 3 · 在 Butea 里配

文档库 → 外部 tab → Obsidian → 填:

- **Obsidian API URL**(默认 \`https://127.0.0.1:27124\`)
- **API Key**(从 Obsidian 复制过来)
- **测试连接**

成功之后,Butea 能读到你 Vault 里的所有 markdown 文件。

### 怎么用

- 在 Obsidian 写笔记 → Butea 外部 tab 看到这个文件 → 点开 → 拉到编辑器编辑
- 编辑保存 → **直接写回 Obsidian 原文件**(不创建副本)
- 在 Obsidian 改了一笔 → Butea 端刷新 → 看到新版

> [!note]
> Local REST API 是**本机**通信(127.0.0.1),不走公网。Vault 的内容不会被 Butea 上传到任何地方。

---

## 资产库 🖼

左侧栏第三个图标。**所有图片在这**——上传的、AI 生成的、拖入的。

详细已经在 [#5 图像](#5) 讲过,这里只补两点:

### 跟文档的关系

文档里的图片用 \`butea-media://xxx\` 引用资产库的图。**多个文档可以引用同一张图**(共用),删图前会提示。

### 容量

浏览器 IndexedDB 默认配额(因浏览器和 OS 不同):

| 浏览器 | 大致配额 |
|--------|---------|
| Chrome / Edge | 单 origin 60% 磁盘空间(可能几 GB 到几十 GB) |
| Firefox | 单 origin 50% 磁盘空间 |
| Safari | 限制较严,几百 MB |

实际上**99% 的用户不会撞到上限**。重度用户(几千张图)可以在资产库里删旧的释放空间。

---

## 文档历史快照(snapshot)

每个文档都有 **手动快照** 机制,记录历史版本。

### 怎么用

文档卡片 hover → 📷 摄像机图标 → 拍一个 "快照",记录当前 markdown 内容 + 时间戳。

### 在哪看

文档库本地 tab,文档卡片底部显示 \`📷 3\`——表示这文档有 3 个快照。

**用途**:

- 大改前先快照(后悔可恢复)
- 不同方向尝试时,A 版快照后再写 B 版
- 想看几小时前的版本

> [!note]
> 自动快照(每天 / 每次切换文档前)是 v0.5 的计划。现在是**手动**。

---

## 实用 tips

- **清空回收站**:文档库 → 回收站 tab → 每个文档单独永久删除。没有"一键清空全部"(防止误操作)
- **批量导出**:暂未支持,v0.5 计划(导出整个 Vault 为 .zip)
- **跨设备**:用 Obsidian 当中转层,Vault 用 Obsidian Sync / iCloud / Syncthing 同步,Butea 读 Vault
- **备份建议**:浏览器 IndexedDB 通过 DevTools → Application 可以**手动导出**(JSON),不放心的话定期导出一份到云盘

---

## 隐私边界

Butea **看不到**:

- 你的文档内容(都在你浏览器)
- 你的 API key(都在你浏览器 localStorage)
- 你的资产(都在 IndexedDB)
- 你的 Obsidian Vault(只在你装 Local REST API 后才能读,且全在本机通信)

Butea **能看到**(因为是开源代码,你可以查):

- 你的请求转发(因为某些 LLM API 不支持浏览器 CORS,要走 Butea 的 \`/api/llm\` 中转。Butea 的服务器是个透明代理,**不存日志、不存 token**)
- 你访问 demo URL 的基础 web 流量(Vercel 默认日志,可关闭)

**自托管选项**:你可以 fork repo,部署到自己的 Vercel,完全 zero trust。详细在 [#8 设置与开源](#8)。

---

*下一站 → #8 设置 · BYOK + 主题 + 写作偏好*
`;
